# 书签工具（自搓）

> **别名**：看板 / Agent 看板 / 书签台 / 随手记 / 手机收发箱
> **一句话**：自己从零搓一个书签工具（收链接、记东西，手机电脑能同步）；开源项目只借零件代码，不在别人应用上整装改造。（2026-09-09 用户拍板）
> **三块**：电脑端 = Agent 看板（寄住在桌宠程序里）／手机端 = 收发箱 APP／中间 = 阿里云收信通道

## 这格里有啥

- `🌞书签工具（总览·为什么这么做）.md` —— **产品总览**：三块各自为什么这么定、否了什么、还没定什么
- `设计思路（看板·聊定的设定·一条三本账）\` —— **电脑端 Agent 看板的设定库**：聊定看板新设定记这里，改看板代码前先来查
- `维修（排障）\` —— **看板修好的毛病**，一个毛病一夹：病根、怎么修的、怎么验的
- `super-productivity\`、`vikunja\` —— **参考代码库**（下下来的不删）：以后借功能、直接拿他们的代码用，省得自己重写；不在里面继续开发
- `super-productivity\`、`vikunja\` —— **参考代码库**（下下来的不删）：以后借功能、直接拿他们的代码用，省得自己重写；不在里面继续开发
- `安装包\` —— Super Productivity 和 Vikunja 的原版安装包，留档对照
- `改造版成品（绿色版·固定入口指这·每次打包覆盖）\` —— 旧方向（Agent 待办）的产物，不再更新，只留对照
- `手机APP_收发箱_Flutter\` —— **手机端收发箱APP（Flutter·仅安卓）**：记一条推到电脑、收回执，出包流程和签名见那格门牌+README
- `通道（阿里云ntfy·部署与收信）\` —— 消息通道层：阿里云邮局部署物 + 电脑端收信模块，先看那格门牌

## 电脑端看板：代码不在这夹

看板和桌宠**共用一个程序**（省一份常驻内存，2026-09-09 拍板），代码留在桌宠的程序仓库里，没搬（路径被开机自启、别的 agent 的命令写死了，搬了全断）：

- **只改看板** → 只动 `..\多Agent桌面宠物（统一通知与权限说明）\app（Electron本体·带git）\src\bookmark\`，进去先读那里的 `AGENTS.md`（验收清单也在那，只跑那几关，桌宠自己的 controls / smoke 不用陪跑）
- **两边共用、改了两边都要验**：程序外壳 `src\app.ts`、`src\shared\`、打包、开机自启入口、Ctrl+R 重开
- 桌宠那摊（幼苗、通知、权限弹窗）跟本项目无关，不用看

## 硬规矩

- 两个参考仓库**只借代码，不整装改造**，别在里面提交新功能
- 借来的代码注意开源协议：自用不商用
- electron-builder 打包 Windows 的 winCodeSign 符号链接坑已预置缓存（`C:\Users\嘉楷\AppData\Local\electron-builder\Cache\winCodeSign\winCodeSign-2.6.0`），别删——以后自己打包 Electron 还用得上
