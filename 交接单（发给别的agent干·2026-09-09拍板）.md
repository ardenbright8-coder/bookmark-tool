# 书签工具 · 总交接单（2026-09-09 用户拍板）

> 🏁 **本单已执行完毕（2026-09-10）**：分工两路全部完工，接手请读同夹《交接单（接手必读·2026-09-10）.md》；本单留作总蓝图与历史依据。

> **这是啥**：书签工具项目的总蓝图+分工单。任何 agent 接手本项目，先读完这份再动手。
> **一句话**：电脑为主的书签/随手记看板（宿主=多Agent桌面宠物业主应用），手机只是收发箱，通道走阿里云自建 ntfy。

## 一、产品是什么（用户原话精神）

- **电脑是主体**：书签台面板常驻在桌宠壳里，随手记东西、存网址、把任务标上「交给谁」（Claude/Grok/Pi 等），看板一目了然。
- **手机只是收发箱**：外面想到啥记一条推给电脑；收回执和看板动态。不装任何第三方应用，功能长在用户自己的 APP 里。
- **风格**：毛玻璃面板。

## 二、总架构（已拍板）

```
【主】Windows 电脑：桌宠壳（常驻）+ 书签台板块（完全隔离的大板块）
      数据老窝：书签台自己的 SQLite，绝不混桌宠事件索引
【辅】手机 APP：收发箱——输入框+发送+本地待发账本+离线重发+收回执
【通道】阿里云 VPS 自建 ntfy（邮局，缓存≥7天，设密码）
      二阶段可选隧道：Cloudflare Tunnel 或 frp（用户有域名和VPS，到时二选一）
【消息链】手机→ntfy→电脑收信写库→面板即时刷新→回「已收录」
【可靠性】手机本地账本先落再发+未确认自动重发；电脑端内容去重
```

## 三、分工与防撞车（两个 agent 并行，物理隔离）

| 板块 | 战场 | 谁 |
|---|---|---|
| 书签台界面区（宿主内） | `..\多Agent桌面宠物（…）\app（Electron本体·带git）\src\bookmark\`：热键呼出面板、本地库、托盘入口 | Agent 1（2026-09-09 已派出） |
| 通道层（本项目夹） | `通道（阿里云ntfy·部署与收信）\`：部署物+手册+收信回执模块 | Agent 2（2026-09-09 已派出） |
| 手机 APP | 本项目夹另建区，选型未定，开工前问用户 | 未派 |

🚨 **边界**：Agent 1 不碰本项目夹，Agent 2 不碰桌宠 app 仓。接线合体是第三步，由在场 agent 协调干。

## 四、硬规矩

- 收信网络模块独立成块，崩了/断网绝不拖累桌宠本体（静默重试+日志）
- 手机端不装任何第三方 APP；ntfy 只是代码里的一行 HTTP
- 密钥/凭证只进本地文件和 Windows 用户环境变量，永不进 Git
- 二阶段才做：桌面最底层嵌入（WorkerW）、隧道、手机 APP
- 纪律：改前备份、门牌先行、Git 只提交不推送、卡住就停下问人
