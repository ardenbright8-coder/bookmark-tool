# 部署手册（照着敲就上）· 阿里云VPS 架 ntfy 邮局

> **这是啥**：从 SSH 登录到手机↔电脑消息跑通的每一条命令。
> **前提**：阿里云安全组已放行 80/443；域名已有一条 A 记录指到 VPS 公网 IP。
> ⚠️ 下面所有 `ntfy.example.com`、账号名、密码都是**占位符**，真值查 `服务器信息（本地·不进git）.md`。

## 一、登录并装 Docker（新机器才需要）

```bash
ssh root@你的VPS_IP
apt update && apt install -y docker.io docker-compose-v2
systemctl enable --now docker
```

## 二、上传部署物

在本机（PowerShell）执行，把 deploy 夹整个推上去：

```powershell
scp -r "本机路径\通道（阿里云ntfy·部署与收信）\deploy" root@你的VPS_IP:/opt/ntfy-deploy
```

## 三、改占位符（上机第一件事）

```bash
cd /opt/ntfy-deploy
sed -i 's/ntfy.example.com/你的真域名/' server.yml Caddyfile
```

## 四、起服务

```bash
docker compose up -d
docker compose ps          # 两个容器都 Up 才算起
docker compose logs --tail 20 ntfy   # 无红色报错
```

## 五、建两个账号（手机发信用 + 电脑收信用）

```bash
# 手机端账号（只准发 up 主题）
docker compose exec ntfy ntfy user add --config /etc/ntfy/server.yml phone-sender
# 电脑端账号（收 up + 发回执）
docker compose exec ntfy ntfy user add --config /etc/ntfy/server.yml pc-receiver

# 精确授权：up 主题手机只写、电脑读写；回执主题只电脑写（topic 名换成真值）
docker compose exec ntfy ntfy access phone-sender bookmark-up w
docker compose exec ntfy ntfy access pc-receiver bookmark-up rw
docker compose exec ntfy ntfy access pc-receiver bookmark-receipt w
```

把账号密码当场填进 `服务器信息（本地·不进git）.md`。

## 六、验证（一条条来，全过才算上）

```bash
# 1. 外部连通（在本机 PowerShell 跑）：应回 {"id":"..."}
curl https://你的真域名/ -u phone-sender:密码 -d "{\"topic\":\"bookmark-up\",\"message\":\"上线测试\"}" -H "Title: 上线"

# 2. 收流（另开一个终端）：上一条消息应在几秒内流出来
curl -s "https://你的真域名/bookmark-up/json?poll=1&since=10m" -u pc-receiver:密码
```

## 七、接电脑端 receiver

见 `..\receiver\README.md`——`start({server, topic, user, pass, onMessage})` 收流、`sendReceipt()` 回执。收到消息手机会弹「已收录」。

## 八、回滚（出问题往回退）

```bash
cd /opt/ntfy-deploy
docker compose down            # 停服，不删数据
# 数据都在这两个目录里，回滚=备份它们再重来：
#   /opt/ntfy-deploy/cache/   （消息缓存）
#   /opt/ntfy-deploy/auth/    （账号库）
docker compose down -v 不可用  # 本方案没用 named volume，防误删没建
```

## 九、升级

改 `docker-compose.yml` 里的镜像版本号（如 v2.11.0 → 新版）→ `docker compose pull && docker compose up -d`。先 `tar czf backup.tgz cache auth` 再动。
