# 手机APP（收发箱·Flutter）· 门牌

> **一句话**：书签工具的手机端——记一条（文字/网址/任务，可贴图）推到电脑，收回执；Flutter + Dart，仅安卓。图只留手机本地，不上传邮局。

## 这夹里有啥

- `bookmark_inbox\` —— APP 源码（三层架构：`lib/core` 通道核心禁沾UI / `lib/ui` 看板界面 / 测试在 `test\`）
- `release\` —— 出包产物（签名 APK + 模拟器截图），**不进 Git**
- `签名信息（本地·不进git）.md` —— 签名凭证说明（本体在 D 盘 keystores，丢了永远不能覆盖升级）

## 想干什么去哪

- 改界面/逻辑 → `bookmark_inbox\lib\`（看板单页在 `ui\agent_board_page.dart`，分组清单在 `ui\agents_roster.dart`）
- 服务器地址/账号 → APP 里点右上角齿轮设置页；真实凭证在 `..\通道（阿里云ntfy·部署与收信）\deploy\服务器信息（本地·不进git）.md`
- **重出包 → `bookmark_inbox\README.md` 的「ASCII 构建链」一节照抄**（中文路径会让 gradle 必炸，别直接在原路径 build）

## 硬规矩

- 🚨 构建只在 ASCII 副本（`D:\a-zhujiyingyong\chengxu\buildwork\bookmark_inbox\`）跑，流程见 README
- 🚨 key.properties / *.jks / 签名信息 / release\ 全不进 Git（已配忽略网）
- 🚨 仅安卓（2026-09-09 拍板）；iOS 未定
- 消息 JSON 带 `type` 字段（note/bookmark/task/cmd），**cmd 收到只存不执行**（云 Agent 留口）
